const form = document.getElementById('form')
const email = document.getElementById('createEmailInput')
const passoword = document.getElementById('createPasswordInput')
const confirmPassoword = document.getElementById('confirmPasswordInput')

form.addEventListener('submit',(e)=>{
    e.preventDefault;
})
